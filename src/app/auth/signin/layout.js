export const metadata = {
  title: 'Sign In — TaperCommunity',
  description: 'Sign in to TaperCommunity, a peer support community for safely tapering psychiatric medications.',
};

export default function SignInLayout({ children }) {
  return children;
}
