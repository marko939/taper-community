export const metadata = {
  title: 'Join TaperCommunity — Peer Support for Medication Tapering',
  description: 'Create your free account to join TaperCommunity. Share your taper journey, connect with peers, and access evidence-based guidance for safely tapering psychiatric medications.',
};

export default function SignUpLayout({ children }) {
  return children;
}
